// NOTE: This file is intentionally not part of the Xcode target.
//
// TalkDaily.xcodeproj lists source files explicitly in project.pbxproj
// (rather than using Xcode 16's file-system-synchronized groups), and this
// change deliberately does not touch project.pbxproj. So the working
// PracticeReminderScheduler implementation lives inline in
// Features/Settings/SettingsView.swift instead of in its own file — that
// file was already part of the build.
//
// This placeholder is kept only so a future search for
// "PracticeReminderScheduler" finds a pointer to where the real code is,
// instead of a second, silently-uncompiled copy.
